{
  "name": "cb-rag-vetorial-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"python api.py\" \"npm run dev --prefix frontend\"",
    "install-all": "npm install && npm install --prefix frontend",
    "backend": "python api.py",
    "frontend": "npm run dev --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
